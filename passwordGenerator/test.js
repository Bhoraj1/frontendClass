var password = "";  // Start with an empty string
var character = "A"; // This is just one character

// Use += to add "A" to the password string
password = character;

console.log(password); // Output: "A"
